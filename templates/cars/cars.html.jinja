{% extends "base.html.jinja" %}
{% block left_panel %}
    <h2>Lista samochoduf</h2>
    <div class="list-group">
        {% for car in cars  %}
            <a type="button" class="list-group-item list-group-item-action" href="{% url 'car' car.id %}">{{car}}</a>
        {% endfor %}
    </div>
    {% for class, category in categories.item %}
        <h3></h3>
    {% endfor %}
{% endblock left_panel %}
